" <enter> in normal mode to insert empty line below
nnoremap <silent> <CR> :call InsertEmptyLineBelow()<CR>

" <shift>+<enter> in normal mode to insert empty line above
nnoremap <silent> <S-CR> :call InsertEmptyLineAbove()<CR>
